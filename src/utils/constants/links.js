export const LINKS = {
  root: '/',
  invite: '/invite/:inviteCode',
  board: '/board/:index',
  userManagement: '/user-management',
  register: '/register',
  screenName: '/screen-name',
};

export default LINKS;
